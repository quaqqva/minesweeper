import footerLayout from './index.html';
import './styles.scss';
import createElement from '../../utils/createElement';

const createFooter = () => createElement(footerLayout);
export default createFooter;
