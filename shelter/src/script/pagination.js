function generatePages(data) {
    let pagesCount = 6, elemsPerPages = 8,
    result = new Array(pagesCount*elemsPerPages);
    for (let i = 0; i < elemsPerPages; i++) {
        for (let j = 0; j < pagesCount; j++) {
            
        }
    }
}